apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name     : myapp5
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    name     : in-cluster
    namespace: app5
  source:
    path   : "HelmCharts/MyChart2"
    repoURL: "git@github.com:ninedend/argocd_9.git"
    targetRevision: HEAD
    helm:
      valueFiles:
      - values_dev.yaml
      parameters:
      - name: "container.image"
        value: nginx

  project: default
  syncPolicy:
    automated:
      prune   : true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true   
